#!/usr/bin/env tpane
# No diagram — tpane auto-generates a 1x3 row from the function names

api()    { while :; do echo api; sleep 1; done; }
worker() { while :; do echo worker; sleep 1; done; }
logs()   { while :; do echo logs; sleep 1; done; }
